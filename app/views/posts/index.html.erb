<%= link_to "記事を作成する", new_post_path %>
<% if @posts.any? %>
<ul>
<% @posts.each do |t| %>
<li><%= link_to t.title, post_path(t.id) %><%= link_to "編集する", edit_post_path(t.id) %><%= link_to "削除する", post_path(t), method: :delete, data: { confirm: "本当に削除しますか?" }  %></li>
<% end %>
</ul>
<% else %>
<p>表示できる記事がありません。</p>
<% end %>